This is Perl module Net::TinyIp::Util::NewFromIfconfig.

INSTALLATION

Net::TinyIp::Util::NewFromIfconfig installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan Net::TinyIp::Util::NewFromIfconfig

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

Net::TinyIp::Util::NewFromIfconfig documentation is available as in POD. So you can do:

    % perldoc Net::TinyIp::Util::NewFromIfconfig

to read the documentation online with your favorite pager.

kuniyoshi
